<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="CentersBarCode.Views.QrScanner"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:zxing="clr-namespace:ZXing.Net.Maui.Controls;assembly=ZXing.Net.MAUI.Controls"
    Title="QrScanner">
    <Grid
        x:Name="qrScannerGrid"
        Grid.Row="0"
        Grid.RowSpan="3"
        BackgroundColor="Black"
        IsVisible="{Binding IsQrScannerVisible}"
        RowDefinitions="Auto,*,*,*">

        <zxing:CameraBarcodeReaderView
            x:Name="cameraView"
            Grid.Row="2"
            BarcodesDetected="CameraView_BarCodeDetected"
            CameraLocation="Rear"
            HeightRequest="150"
            HorizontalOptions="Fill"
            IsDetecting="True"
            VerticalOptions="Fill"
            WidthRequest="300" />

        <StackLayout
            Grid.Row="2"
            Margin="0,-30,0,0"
            HorizontalOptions="Center"
            Orientation="Horizontal"
            VerticalOptions="Start">
            <Label
                Grid.Row="1"
                Grid.Column="0"
                Margin="0,0,0,0"
                FontSize="14"
                HorizontalOptions="Center"
                Text="Position Barcode inside frame"
                TextColor="White" />

            <Button
                Grid.Row="1"
                Margin="0,-10,0,0"
                Padding="0"
                BackgroundColor="Transparent"
                BorderColor="Transparent"
                Clicked="ToggleFlash_Clicked"
                FontSize="24"
                HeightRequest="40"
                HorizontalOptions="Center"
                Text="💡"
                WidthRequest="40" />
        </StackLayout>
        <Grid
            Grid.Row="2"
            ColumnDefinitions="*,Auto"
            HorizontalOptions="Center"
            RowSpacing="10"
            VerticalOptions="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Frame
                Grid.Row="1"
                Grid.Column="0"
                Padding="2"
                BackgroundColor="Transparent"
                BorderColor="White"
                CornerRadius="10"
                HasShadow="False"
                HeightRequest="150"
                HorizontalOptions="Center"
                WidthRequest="250" />
        </Grid>
        <!--  Controls Grid  -->
        <Grid
            Grid.Row="0"
            Padding="20"
            RowDefinitions="Auto,*,Auto">
            <!--  Top controls  -->
            <VerticalStackLayout
                Grid.Row="0"
                HorizontalOptions="Center"
                Spacing="10">
                <Label
                    BackgroundColor="Transparent"
                    FontSize="24"
                    HorizontalTextAlignment="Center"
                    IsVisible="{Binding Source={RelativeSource Self}, Converter={StaticResource IsAttachCardViewModel}}"
                    Text="{Binding TeacherName}"
                    TextColor="#FFFFFF" />
                <Label
                    BackgroundColor="Transparent"
                    FontSize="24"
                    HorizontalTextAlignment="Center"
                    IsVisible="{Binding Source={RelativeSource Self}, Converter={StaticResource IsAttachCardViewModel}}"
                    Text="{Binding PhoneNumber}"
                    TextColor="#FFFFFF" />
                <Label
                    BackgroundColor="Transparent"
                    FontSize="24"
                    HorizontalTextAlignment="Center"
                    IsVisible="{Binding Source={RelativeSource Self}, Converter={StaticResource IsMainViewModel}}"
                    Text="{Binding SelectedCenter}"
                    TextColor="#FFFFFF" />
            </VerticalStackLayout>
        </Grid>
        <!--  Controls Grid  -->
        <Grid
            Grid.Row="3"
            Padding="20"
            RowDefinitions="Auto,*,Auto">
            <!--  Bottom controls  -->
            <VerticalStackLayout
                Grid.Row="2"
                HorizontalOptions="Center"
                Spacing="0">
                <Button
                    BackgroundColor="Red"
                    Clicked="CloseQr"
                    Command="{Binding CloseQrScannerCommand}"
                    HorizontalOptions="Center"
                    Text="Close Scanner"
                    TextColor="White" />
            </VerticalStackLayout>
        </Grid>
    </Grid>
</ContentPage>